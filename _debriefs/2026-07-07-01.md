---
session-date: 2026-07-07
sequence: 1
first-commit: b5eeb97
last-commit: 60297be
duration: ~15h35m
---

# Session Debrief — 2026-07-07 #1

Sprint 0 of the agent rail, commissioned and completed in one session: the
workflow was designed, three slices went seed → gated ideation → TDD →
adversarial validation → live demo → direct merge, and the rail rendered its
first agent rows. Preceded by the docking hardening pass (F1/F2/F3/F9 fixed,
F4 refuted) on the same day.

## Shipped (direct merges — no remote/PRs)

- **2n** `plugin-pipe-unblock` — merge `9307e14`. Wedge budget bounds a
  queued CLI pipe's wait to one wedge (~12s live vs ~90s); ideation refuted
  the seed mechanism at v0.44.1 source and shipped the honest fix instead.
- **fv** `grout-skeleton` — merge `5ac875a`. One-shot Go binary: one
  agentsview session + the vendored subspace gate log → the two pinned row
  kinds on `agent-event`, per-row kill timer, hermetic suite.
- **5t** `plugin-rows-section` — merge `60297be`. AGENTS/GATES sections with
  zero footprint while empty, cwd binding (unbound rendered, never guessed),
  per-kind clicks (focus / float subspace-tui), F8 exit-nav fix; 104→130
  tests.

## Filed (backlog)

- **ka** `grout-session-states` — map agentsview state vocabulary onto the
  four plugin markers (live rows currently blank) + fix grout's cwd-relative
  default gate-log path. (0.9)
- **yb** `grout-sse-daemon` — long-running grout on SSE `data_changed`;
  sessions for real; exit: CL stops alt-tabbing. (0.8)
- **hj** `rail-row-lifecycle` — row expiry (audit: load-bearing before any
  chatty grout) + unbind debounce over flaky cwd reads. (0.7)
- Sprint-0 seeds `2n`/`fv`/`5t` — filed and shipped same session.

## Non-PR commits (workflow-only)

Scaffolding and stage records that don't belong to a merge:

- `57ef384` commission (dev shape, subspace review channel, split-root state)
- `e696993` demo-as-gate adaptation + spacedock-v1 proof-discipline steals
  (value-measuring AC rule, prose-grep ban, spot-check-first, explicit
  approval, park-for-demo posture)
- `134d2a7` sprint plan (`docs/plan-agent-rail.md`) · `09a4710` gitignore
- Docking arc (pre-workflow, same session, on main): `b5eeb97` F1
  single-line rail detect · `ed4fb8b` F2 foreign-swap replace · `9b552bd` F3
  floating-visible gate · `e8e6584` F4 refuted + F9/F10 logged · `e6d2070`
  F9 three-bar chrome allowlist
- State branch, consolidated per slice: ideation `da28bd0`/`86a1944`/
  `2162687`; implementation reports `9e3b29a`/`27e62fe`/`0445449`;
  validation `3f185a4`/`b4dd549`/`8141dc0`; demo addendum `d5d9d6f`;
  archives by merge guard `5981db2`/`7956500`/`8422d5c`.

All other session commits are rolled up in the three merges above.

## Decisions

- Demo-as-gate: backlog ungated (FO advances in sprint-plan order); the
  validation gate is CL's live demo in a fresh session; parking a slice
  demo-ready to wait for CL is correct posture, not a stall.
- The four grill calls: identity binding in the plugin; grout in Go under
  `grout/`; two typed row kinds over one pipe name `agent-event`; gate-log
  discovery by config of globs (canonical dir deferred).
- `main` fast-forwarded to the wip tip and made the dispatch trunk; push and
  remote deferred until after the shakedown (CL).
- Pipe-unblock's approved reversal: no `ReadCliPipes` grant, no explicit
  unblock — wedge budget on the poll path instead (refutation source-cited).
- F8 rider kept inside rows-section (same lines rewritten).
- The binding component is named **grout** (binds tiles into a mosaic);
  the rail is zaphod's own coinage (nav-rail pattern), not a zellij term.
- Deploys of candidate wasm go via cp-to-production-path, never a worktree
  `install.sh` (plugin identity = URL + config block, byte-for-byte).

## Issues — Workflow

- State-checkout directory deleted mid-session by an unidentified actor
  (recovered losslessly: every change was committed to the orphan branch;
  `git worktree remove --force` + re-add). FO cwd drift into a worktree
  compounded diagnosis — the stay-at-root rule exists for a reason.
- Demo-script defects found live (all three now encoded as rules): worktree
  `install.sh` splits plugin identity from the config.kdl keybind; `go run
  ./grout` fails from any dir (module root is `grout/`); grout's default
  gate-log path is cwd-relative and never resolves under `go run .`.
- Permission-drift silent death: a deploy that widens the permission set
  invalidates the cached grant and SPEC #7's prompt-never-renders parks the
  plugin dead (events denied, toggles "parked as bootstrap"). Remedies
  proven: re-attach renders the prompt; seeding permissions.kdl works.
  Task #18 tracks the SPEC addendum + demo-script rule.
- Live AGENTS markers carry no signal (grout emits `termination_status`
  verbatim) — seeded as `ka`. Raw per-read `pane_cwd` is flaky (null one
  read, real the next) — stale-not-blank smooths it; unbind debounce seeded
  as `hj`.

## Issues — Spacedock

1. `spacedock state ready` resolves init/fallback paths relative to cwd
   (invoked from inside a worktree it proposed re-adding the state checkout
   under the worktree) and hard-fails on a local-only state branch by
   attempting `git fetch origin` with no local-branch fallback. — not yet
   filed.
2. `merge guard <slug>` reports "entity not found" from a foreign cwd even
   with `--workflow-dir` passed; succeeds from the project root. — not yet
   filed.

## Observations

- Step-zero mechanism verification paid three times in one day (F4 refuted
  before shipping a regression; pipe-unblock's seed refuted at source; F3
  confirmed before fixing) — carrying SUSPECTED/CONFIRMED labels on findings
  and briefing confirm-first earns its keep.
- The demo-as-gate caught five real defects (three script bugs, SPEC #7
  live, the blank-marker gap) that offline validation could never see; the
  interactive-AC split (agent reproduces offline, CL settles interactive)
  held up in practice.
- Ensigns idle without pushing final reports unless the brief names the
  SendMessage step explicitly — now standard in dispatch briefs.
- The validator-promotes-a-probe-test pattern (born-green mutation finding →
  ~25-line integration test landed pre-merge) is a cheap, repeatable way to
  convert audit findings into permanent suite strength.

## What's Next

- Dispatchable (score order): `ka` grout-session-states → `yb`
  grout-sse-daemon → `hj` rail-row-lifecycle. Say "run sprint 1".
- Captain's desk: glossary bless (proposal drafted in-session) · PRD voice
  pass (`docs/prd-agent-rail.md`, still uncommitted) · upstream 8th-slot
  call (SPEC #7 now evidence-backed; docket in task #5) · F5-F8/F10 triage
  (task #3) · permission-drift doc task (#18).
- Environment: private-journal MCP down all session (insight capture queued
  in task #7) · no git remote — state branch and debriefs are local-only.
